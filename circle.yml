machine:
  java:
    version: oraclejdk7
  pre:
    - wget https://github.com/Cnly/Crafter/archive/master.zip -O crafter.zip
    - unzip crafter.zip
    - mvn clean install:
        pwd: $HOME/Crafter-master/Crafter
general:
  build_dir: WowSuchCleaner
  artifacts:
    - "target/WowSuchCleaner-*.jar"
